/**
 * `@hookorama/cli` — public barrel.
 *
 * Placeholder in PR 1. Real exports (and the `hookorama` binary
 * entry point) land in PR 4 with the CLI-surface ADR.
 */

export const PLACEHOLDER = true as const;