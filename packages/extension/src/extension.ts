/**
 * `@hookorama/extension` — VS Code extension entry.
 *
 * Placeholder in PR 1 (the bootstrap). The real `activate` /
 * `deactivate` land in Phase 4 once the supervisor and the wire
 * protocol exist. The placeholder keeps the build green and
 * makes the import surface obvious to future contributors.
 *
 * No `vscode` imports here on purpose; this file is meant to be
 * readable before the extension host is wired up.
 */

export const PLACEHOLDER = true as const;